# secrets & runtime state — NEVER commit (the emotos47 lesson)
.env
*.env
!.env.example
config.yaml
/data/
*.db
*.sqlite*
*.log

# build artifacts
/bourse
/bin/
dist/

# ML model artifacts — never commit weights (bloats history forever)
*.onnx
*.bin
*.safetensors
*.pt
data/models/
**/.cache/huggingface/
